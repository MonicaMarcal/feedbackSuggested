import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()]
})

//Aqui é uma configuração
//que diz que estamos utilizando o plugin do react